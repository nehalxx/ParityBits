
  # High-Fidelity Interaction Flow

  This is a code bundle for High-Fidelity Interaction Flow. The original project is available at https://www.figma.com/design/OY19fAGSKYd0ZKcet3EoWF/High-Fidelity-Interaction-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  